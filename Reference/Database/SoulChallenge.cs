//=============================================================================
// DO NOT MODIFY THIS FILE MANUALLY!!
// THIS FILE IS GENERATED BY LUA DATABASE TOOL.
// IF YOU HAVE ANY QUESTIONS PLEASE CONTACT LUJIAN.
//=============================================================================

using System;
using System.Collections.Generic;
using Common;
#if USE_NATIVE_LUA
using NLuaState = NativeLua.NLuaState;
using LuaType = NativeLua.LuaDLL;
using LuaTinker = NativeLua.LuaTinker;
using LuaDatabase = NativeLua.LuaDatabase;
using LuaTableHandle = System.Int64;
using LuaStateHandle = System.IntPtr;
#else
using NLuaState = UniLua.ILuaState;
using LuaType = UniLua.LuaType;
using LuaTinker = UniLuax.LuaTinker;
using LuaDatabase = UniLuax.LuaDatabase;
using LuaTableHandle = System.Object;
using LuaStateHandle = UniLua.ILuaState;
#endif

namespace Database {
	public partial class SoulChallenge {
		const string DBName = "soul_challenge";
		#region members
		private int m_id = 0;
		private string m_description = String.Empty;
		private int m_finish_type = 0;
		private int m_model_angle = 0;
		private int m_monster_show = 0;
		private int m_standard_combat = 0;
		private int m_time = 0;
		#endregion
		#region caches
		private static IKeys m_ids = null;
		private static Common.Collections.Generic.Lit.Dictionary<int, SoulChallenge> m_idCache = new Common.Collections.Generic.Lit.Dictionary<int, SoulChallenge>();
		#endregion
		#region properties
		public int id { get { return m_id; } }
		public string description { get { return m_description; } }
		public int finish_type { get { return m_finish_type; } }
		public int model_angle { get { return m_model_angle; } }
		public int monster_show { get { return m_monster_show; } }
		public int standard_combat { get { return m_standard_combat; } }
		public int time { get { return m_time; } }
		#endregion
		#region method
		static LuaTableHandle __this = default( LuaTableHandle );
		static SoulChallenge __default = null;
		static int __hash = 0;
		static bool __masked = false;

		static SoulChallenge() {
			LuaDatabase.datasetUnloadEvent += OnUnload;
			if ( Load() ) {
				DBLoadedItems.MarkAutoLoaded( DBName, typeof( SoulChallenge ) );
			}
		}

		public static String DbName {
			get { return DBName; }
		}

		public static Common.Collections.Generic.Lit.Dictionary<int, SoulChallenge> idCacheData {
			get { return m_idCache; }
		}

		public static void Touch() {
		}

		public static bool EnsureLoaded() {
			return Load();
		}

		static void ReadDefault() {
			var db = LuaDatabase.sharedInstance;
			var L = db.GetLuaState();
			var top = db.GetLuaTop();
			try {
				db.PushDataset( DBName );
				if ( L.IsTable( -1 ) ) {
					L.GetMetaTable( -1 );
					if ( L.IsTable( -1 ) ) {
						__this = ( LuaTableHandle )L.ToObject( -1 );
						var table = new LuaTinker.Table( L, -1 );
						var subTable = table.GetSubTable( "__default" );
						__masked = table.Get<bool>( "__masked", false );
						__hash = table.Get<int>( "__hash", 0 );
						if ( subTable != LuaTinker.Table.Empty && __hash == VersionHash ) {
							using ( var q = db.CreateQuery( subTable.index ) ) {
								__default = ReadOne( q, default( int ) );
							}
						} else {
							if ( __hash != 0 ) {
								ULogFile.sharedInstance.LogError( "Database {0} version mismatch, reader = 0x{1:x8}, data = 0x{2:x8}, masked = {3} ", DBName, VersionHash, __hash, __masked );
							}
							__masked = false;
						}
					}
				}
			} finally {
				db.SetLuaTop( top );
			}
		}

		public static bool Load() {
			bool alreadyLoaded;
			var r = LuaDatabase.sharedInstance.Load( DBName, out alreadyLoaded );
			if ( r && !alreadyLoaded ) {
				ReadDefault();
			}
			return r;
		}

		public static bool IsLoaded() {
			return LuaDatabase.sharedInstance.IsLoaded( DBName );
		}

		public static bool Unload() {
			return LuaDatabase.sharedInstance.Unload( DBName );
		}

		public static bool Reload() {
			bool ret = LuaDatabase.sharedInstance.Reload( DBName );
			ReadDefault();
			ClearCache();
			return ret;
		}

		static void OnUnload( LuaStateHandle L, LuaTableHandle tablePtr, String name ) {
			if ( name == DBName && tablePtr == __this ) {
				__this = default( LuaTableHandle );
				__default = null;
				__hash = 0;
				__masked = false;
			}
		}

		public static int CacheAll() {
			ClearCache();
			var db = LuaDatabase.sharedInstance;
			var L = db.GetLuaState();
			var top = db.GetLuaTop();
			try {
				db.PushDataset( DBName );
				using ( var gbuff = Common.GlobalBuffer<int>.Get() ) {
					LuaDatabase.LuaTable_iForEach( L,
						( _L, id ) => {
							using ( var q = db.CreateQuery( -1, DBName ) ) {
								var value = ReadOne( q, id );
								if ( value != null ) {
									m_idCache.Add( id, value );
									gbuff.Add( id );
								}
							}
						}
					);
					gbuff.Sort();
					m_ids = new IKeys( gbuff.ToList() );
				};
			} finally {
				db.SetLuaTop( top );
			}
			return m_idCache.Count;
		}

		public static void ClearCache() {
			m_idCache.Clear();
			m_ids = null;
		}

		public static int Sweep( bool force = false ) {
			return 0;
		}

		public static IKeys GetKeys( Comparison<int> pred = null ) {
			if ( m_ids == null ) {
				m_ids = new IKeys( LuaDatabase.sharedInstance.GetDatasetIKeys( DBName, true, pred ) );
			}
			var ids = m_ids;
			if ( pred != null ) {
				int count = m_ids.Count;
				var _ids = new List<int>( count );
				for ( int i = 0; i < count; ++i ) {
					_ids.Add( m_ids[i] );
				}
				_ids.Sort( pred );
				ids = new IKeys( _ids );
			}
			return ids;
		}

		static SoulChallenge ReadOne( LuaDatabase.Query q, int id ) {
			if ( q.IsTable() ) {
				var temp = new SoulChallenge();
				var __mask = DBCache.LoadCurrentFieldMask( q, __masked );
				temp.m_id = id;
				temp.m_description = __mask[ 0 ] ? Dataset.DecodeToLocalizedString( q, DBCache.FID_description, 11 ) : __default.m_description;
				temp.m_finish_type = __mask[ 1 ] ? q.GetInt( DBCache.FID_finish_type, 11 ) : __default.m_finish_type;
				temp.m_model_angle = __mask[ 2 ] ? q.GetInt( DBCache.FID_model_angle, 11 ) : __default.m_model_angle;
				temp.m_monster_show = __mask[ 4 ] ? q.GetInt( DBCache.FID_monster_show, 12 ) : __default.m_monster_show;
				temp.m_standard_combat = __mask[ 5 ] ? q.GetInt( DBCache.FID_standard_combat, 15 ) : __default.m_standard_combat;
				temp.m_time = __mask[ 6 ] ? q.GetInt( DBCache.FID_time, 4 ) : __default.m_time;
				SoulChallengeEx.IsValid_monster_1 = __mask[ 3 ];
				if ( SoulChallengeEx.onLoadComplexField_monster_1 != null ) {
					SoulChallengeEx.onLoadComplexField_monster_1( temp, q );
				}
				return temp;
			}
			return null;
		}

 		public static SoulChallenge Get( int id ) {
			if ( id == 0 ) {
				return null;
			}
			SoulChallenge ret = null;
			if ( m_idCache.TryGetValue( id, out ret ) ) {
				return ret;
			}
			var db = LuaDatabase.sharedInstance;
			int tableIndex = db.PushDatasetValue( DBName, id );
			var L = db.GetLuaState();
			if ( L.IsTable( tableIndex ) == false ) {
			    L.Remove( tableIndex );
			    Common.UDebug.LogError( "{0}[{1}] is nil", DBName, id );
			    return null;
			}
			using ( var q = db.CreateQuery( tableIndex, DBName ) ) {
				var temp = ReadOne( q, id );
				if ( temp != null ) {
					m_idCache.Add( id, temp );
					return temp;
				}
			}
			return null;
		}

		const int VersionHash = 0x484eeb5e;
		#endregion
	}

	public partial class SoulChallengeEx {
		public delegate void LoadComplexField( SoulChallenge data, LuaDatabase.Query q );
		internal static bool IsValid_monster_1 = true;
		static LoadComplexField OnLoadComplexField_monster_1 = null;
		public static LoadComplexField onLoadComplexField_monster_1 {
			get {
				return OnLoadComplexField_monster_1;
			}
		}
	}
}
//EOF
